# HG changeset patch
# Parent e2f188194a8323f69a82adeac2b0bfadfe81eff3
# User Ben Kelly <ben@wanderview.com>
Bug 1110487 P3 interdiff 003 use override on AddRef/Release


diff --git a/dom/cache/Context.cpp b/dom/cache/Context.cpp
--- a/dom/cache/Context.cpp
+++ b/dom/cache/Context.cpp
@@ -124,17 +124,17 @@ private:
     nsresult Result() const { return mResult; }
 
   private:
     ~SyncResolver() { }
 
     bool mResolved;
     nsresult mResult;
 
-    NS_INLINE_DECL_REFCOUNTING(Context::QuotaInitRunnable::SyncResolver)
+    NS_INLINE_DECL_REFCOUNTING(Context::QuotaInitRunnable::SyncResolver, override)
   };
 
   ~QuotaInitRunnable()
   {
     MOZ_ASSERT(mState == STATE_COMPLETE);
     MOZ_ASSERT(!mContext);
     MOZ_ASSERT(!mQuotaIOThreadAction);
   }
